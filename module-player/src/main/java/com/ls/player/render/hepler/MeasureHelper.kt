package com.ls.player.render.hepler

import com.ls.player.constants.ScreenType

class MeasureHelper {

    private var measureWidth = 0
    private var measureHeight = 0

    var videoRotationDegree = 0
    var screenType: ScreenType = ScreenType.SCREEN_DEFAULT

    fun doMeasure(widthMeasureSpec: Int, heightMeasureSpec: Int) {

    }
}